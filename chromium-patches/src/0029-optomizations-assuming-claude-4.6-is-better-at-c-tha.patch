From a24d4b3f7ac1eda015c5fe53219546c530c3dc35 Mon Sep 17 00:00:00 2001
From: Leo Rodarte <leorodarte07@gmail.com>
Date: Thu, 5 Mar 2026 19:41:00 -0800
Subject: [PATCH 29/32] optomizations (assuming claude 4.6 is better at c++
 than me)

---
 components/nodriver4j/default_voices.cc       |   7 -
 components/nodriver4j/default_voices.h        |   3 +-
 components/nodriver4j/font_whitelist.cc       |  12 +-
 components/nodriver4j/webrtc_ip_spoofer.h     | 174 ++++++++++--------
 .../geolocation/location_provider_manager.cc  |  78 ++++----
 .../canvas2d/canvas_2d_recorder_context.cc    |   3 +-
 .../modules/canvas/canvas2d/canvas_path.cc    |  31 ++--
 .../modules/mediastream/media_devices.cc      |  33 +++-
 .../modules/speech/speech_synthesis.cc        |  26 +--
 .../webgl/webgl2_rendering_context_base.cc    |   5 +-
 .../webgl/webgl_rendering_context_base.cc     |   5 +-
 .../platform/fonts/shaping/harfbuzz_shaper.cc |   1 -
 12 files changed, 218 insertions(+), 160 deletions(-)

diff --git a/components/nodriver4j/default_voices.cc b/components/nodriver4j/default_voices.cc
index 697b092408..3ff4c6afc0 100644
--- a/components/nodriver4j/default_voices.cc
+++ b/components/nodriver4j/default_voices.cc
@@ -192,11 +192,4 @@ base::span<const DefaultVoiceEntry> VoicesForPlatform(
   return {};
 }
 
-float FakeSpeechCharsPerSecond() {
-  // ~150 words per minute ≈ 12.5 characters per second.
-  // Matches the average English speaking rate and is consistent
-  // with Camoufox's default value.
-  return 12.5f;
-}
-
 }  // namespace nodriver4j
\ No newline at end of file
diff --git a/components/nodriver4j/default_voices.h b/components/nodriver4j/default_voices.h
index 26cd9fb81a..c704021a55 100644
--- a/components/nodriver4j/default_voices.h
+++ b/components/nodriver4j/default_voices.h
@@ -34,8 +34,7 @@ base::span<const DefaultVoiceEntry> VoicesForPlatform(
 
 // Returns the default fake-TTS speaking rate in characters per second.
 // Used to calculate the duration of fake speech completion events.
-COMPONENT_EXPORT(NODRIVER4J)
-float FakeSpeechCharsPerSecond();
+inline constexpr float kFakeSpeechCharsPerSecond = 12.5f;
 
 }  // namespace nodriver4j
 
diff --git a/components/nodriver4j/font_whitelist.cc b/components/nodriver4j/font_whitelist.cc
index c3c0315606..a9986a3982 100644
--- a/components/nodriver4j/font_whitelist.cc
+++ b/components/nodriver4j/font_whitelist.cc
@@ -14,9 +14,9 @@ namespace {
 
 // Generic CSS families — always allowed, never blocked.
 const char* const kGenericFamilies[] = {
-    "serif", "sans-serif", "monospace", "cursive", "fantasy",
-    "system-ui", "ui-serif", "ui-sans-serif", "ui-monospace", "ui-rounded",
-    "math", "emoji", "fangsong",
+    "sans-serif", "serif", "monospace", "system-ui", "cursive",
+    "fantasy", "emoji", "ui-sans-serif", "ui-serif", "ui-monospace",
+    "ui-rounded", "math", "fangsong",
 };
 
 // PASTE FONT LISTS BELOW — one string literal per font name.
@@ -890,13 +890,13 @@ bool IsFontBlocked(const std::string& family_name) {
   if (!whitelist)
     return false;
 
-  // Generic CSS families are always allowed.
-  std::string lower = base::ToLowerASCII(family_name);
+  // Generic CSS families are always allowed — check before allocating.
   for (const char* generic : kGenericFamilies) {
-    if (lower == generic)
+    if (base::EqualsCaseInsensitiveASCII(family_name, generic))
       return false;
   }
 
+  std::string lower = base::ToLowerASCII(family_name);
   return whitelist->find(lower) == whitelist->end();
 }
 
diff --git a/components/nodriver4j/webrtc_ip_spoofer.h b/components/nodriver4j/webrtc_ip_spoofer.h
index fec875ed96..f18bf0aee2 100644
--- a/components/nodriver4j/webrtc_ip_spoofer.h
+++ b/components/nodriver4j/webrtc_ip_spoofer.h
@@ -10,11 +10,11 @@
 #ifndef COMPONENTS_NODRIVER4J_WEBRTC_IP_SPOOFER_H_
 #define COMPONENTS_NODRIVER4J_WEBRTC_IP_SPOOFER_H_
 
-#include <sstream>
 #include <string>
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/no_destructor.h"
 #include "components/nodriver4j/nodriver4j_switches.h"
 
 namespace nodriver4j {
@@ -23,24 +23,27 @@ class WebRtcIpSpoofer {
  public:
   WebRtcIpSpoofer() = delete;
 
-  // Returns true if either --webrtc-ip4 or --webrtc-ip6 is set.
-  static inline bool IsActive() {
-    const auto* cmd = base::CommandLine::ForCurrentProcess();
-    return cmd->HasSwitch(switches::kWebrtcIP4) ||
-           cmd->HasSwitch(switches::kWebrtcIP6);
+  private:
+  struct Config {
+    std::string ipv4;
+    std::string ipv6;
+    bool active;
+  };
+
+  static inline const Config& GetConfig() {
+    static const base::NoDestructor<Config> config([]() -> Config {
+      const auto* cmd = base::CommandLine::ForCurrentProcess();
+      std::string v4 = cmd->GetSwitchValueASCII(switches::kWebrtcIP4);
+      std::string v6 = cmd->GetSwitchValueASCII(switches::kWebrtcIP6);
+      return {v4, v6, !v4.empty() || !v6.empty()};
+    }());
+    return *config;
   }
 
-  // Returns the IPv4 spoof address, or empty string if not set.
-  static inline std::string SpoofIPv4() {
-    return base::CommandLine::ForCurrentProcess()
-        ->GetSwitchValueASCII(switches::kWebrtcIP4);
-  }
-
-  // Returns the IPv6 spoof address, or empty string if not set.
-  static inline std::string SpoofIPv6() {
-    return base::CommandLine::ForCurrentProcess()
-        ->GetSwitchValueASCII(switches::kWebrtcIP6);
-  }
+ public:
+  static inline bool IsActive() { return GetConfig().active; }
+  static inline const std::string& SpoofIPv4() { return GetConfig().ipv4; }
+  static inline const std::string& SpoofIPv6() { return GetConfig().ipv6; }
 
   // Returns true if the address contains a colon (IPv6 notation).
   static inline bool IsIPv6(const std::string& ip) {
@@ -95,38 +98,41 @@ class WebRtcIpSpoofer {
     if (!IsActive() || candidate.empty())
       return candidate;
 
-    // Tokenize by whitespace
-    std::vector<std::string> tokens;
-    std::istringstream iss(candidate);
-    std::string token;
-    while (iss >> token) {
-      tokens.push_back(token);
+    // Find connection address at token index 4
+    size_t pos = 0;
+    for (int i = 0; i < 4; ++i) {
+      pos = candidate.find(' ', pos);
+      if (pos == std::string::npos)
+        return candidate;
+      ++pos;
     }
-
-    // Need at least 5 tokens to contain an address field
-    if (tokens.size() < 5)
+    size_t end = candidate.find(' ', pos);
+    if (end == std::string::npos)
       return candidate;
 
-    // Connection address is always at index 4
-    tokens[4] = ReplacementForIP(tokens[4]);
+    std::string result = candidate;
+    std::string addr = result.substr(pos, end - pos);
+    std::string replacement = ReplacementForIP(addr);
+    if (replacement != addr)
+      result.replace(pos, end - pos, replacement);
 
     // Replace related address if present (follows "raddr" keyword)
-    for (size_t i = 0; i + 1 < tokens.size(); ++i) {
-      if (tokens[i] == "raddr") {
-        tokens[i + 1] = ReplacementForIP(tokens[i + 1]);
-      }
+    size_t raddr_pos = result.find(" raddr ");
+    if (raddr_pos != std::string::npos) {
+      size_t addr_start = raddr_pos + 7;
+      size_t addr_end = result.find(' ', addr_start);
+      if (addr_end == std::string::npos)
+        addr_end = result.size();
+      addr = result.substr(addr_start, addr_end - addr_start);
+      replacement = ReplacementForIP(addr);
+      if (replacement != addr)
+        result.replace(addr_start, addr_end - addr_start, replacement);
     }
 
-    // Reconstruct the candidate string
-    std::string result;
-    for (size_t i = 0; i < tokens.size(); ++i) {
-      if (i > 0)
-        result += ' ';
-      result += tokens[i];
-    }
     return result;
   }
 
+
   // Replaces IP addresses in a full SDP body.
   //
   // Processes three types of SDP lines:
@@ -140,56 +146,80 @@ class WebRtcIpSpoofer {
     if (!IsActive() || sdp.empty())
       return sdp;
 
-    std::istringstream iss(sdp);
-    std::ostringstream oss;
-    std::string line;
+    std::string result;
+    result.reserve(sdp.size());
+
+    size_t pos = 0;
+    while (pos < sdp.size()) {
+      size_t eol = sdp.find('\n', pos);
+      if (eol == std::string::npos)
+        eol = sdp.size();
+
+      bool is_candidate = (sdp.compare(pos, 12, "a=candidate:") == 0);
+      bool is_connection = (!is_candidate && sdp.compare(pos, 2, "c=") == 0);
+      bool is_origin = (!is_candidate && !is_connection &&
+                        sdp.compare(pos, 2, "o=") == 0);
+
+      if (!is_candidate && !is_connection && !is_origin) {
+        // Unmodified line — append raw bytes including line ending
+        size_t chunk_end = (eol < sdp.size()) ? eol + 1 : eol;
+        result.append(sdp, pos, chunk_end - pos);
+        pos = (eol < sdp.size()) ? eol + 1 : sdp.size();
+        continue;
+      }
 
-    while (std::getline(iss, line)) {
-      // Preserve \r if present (SDP uses \r\n line endings)
-      bool had_cr = (!line.empty() && line.back() == '\r');
-      if (had_cr)
-        line.pop_back();
+      // Extract line content without \r\n
+      size_t line_end = eol;
+      bool has_cr = (line_end > pos && sdp[line_end - 1] == '\r');
+      if (has_cr)
+        --line_end;
+      std::string line = sdp.substr(pos, line_end - pos);
 
-      if (line.compare(0, 12, "a=candidate:") == 0) {
+      if (is_candidate) {
         line = SpoofCandidate(line);
 
-      } else if (line.compare(0, 2, "c=") == 0) {
-        // Connection line: c=IN IP4 <address> or c=IN IP6 <address>
+      } else if (is_connection) {
+        // c=IN IP4 <address> — address after second space
         auto space1 = line.find(' ');
         if (space1 != std::string::npos) {
           auto space2 = line.find(' ', space1 + 1);
           if (space2 != std::string::npos) {
             std::string addr = line.substr(space2 + 1);
-            line = line.substr(0, space2 + 1) + ReplacementForIP(addr);
+            std::string replacement = ReplacementForIP(addr);
+            if (replacement != addr)
+              line.replace(space2 + 1, std::string::npos, replacement);
           }
         }
 
-      } else if (line.compare(0, 2, "o=") == 0) {
-        // Origin line: o=<user> <sess-id> <sess-ver> IN IP4/IP6 <address>
-        // Address is at token index 5
-        std::vector<std::string> tokens;
-        std::istringstream liss(line);
-        std::string tok;
-        while (liss >> tok)
-          tokens.push_back(tok);
-        if (tokens.size() >= 6) {
-          tokens[5] = ReplacementForIP(tokens[5]);
-          line.clear();
-          for (size_t i = 0; i < tokens.size(); ++i) {
-            if (i > 0)
-              line += ' ';
-            line += tokens[i];
-          }
+      } else if (is_origin) {
+        // o=<user> <id> <ver> IN IP4 <address> — token index 5
+        size_t tpos = 0;
+        for (int i = 0; i < 5; ++i) {
+          tpos = line.find(' ', tpos);
+          if (tpos == std::string::npos)
+            break;
+          ++tpos;
+        }
+        if (tpos != std::string::npos && tpos < line.size()) {
+          size_t tend = line.find(' ', tpos);
+          if (tend == std::string::npos)
+            tend = line.size();
+          std::string addr = line.substr(tpos, tend - tpos);
+          std::string replacement = ReplacementForIP(addr);
+          if (replacement != addr)
+            line.replace(tpos, tend - tpos, replacement);
         }
       }
 
-      oss << line;
-      if (had_cr)
-        oss << '\r';
-      oss << '\n';
+      result += line;
+      if (has_cr)
+        result += '\r';
+      if (eol < sdp.size())
+        result += '\n';
+      pos = (eol < sdp.size()) ? eol + 1 : sdp.size();
     }
 
-    return oss.str();
+    return result;
   }
 };
 
diff --git a/services/device/geolocation/location_provider_manager.cc b/services/device/geolocation/location_provider_manager.cc
index 87db2a495b..9038a8ebb0 100644
--- a/services/device/geolocation/location_provider_manager.cc
+++ b/services/device/geolocation/location_provider_manager.cc
@@ -134,41 +134,51 @@ void LocationProviderManager::OnPermissionGranted() {
 
 void LocationProviderManager::StartProvider(bool enable_high_accuracy) {
   // --- BEGIN geolocation spoofing ---
-  const std::string geo_value =
-      base::CommandLine::ForCurrentProcess()->GetSwitchValueASCII(
-          "fingerprint-geolocation");
-  if (!geo_value.empty()) {
+  struct GeoSpoof {
+    double lat;
+    double lon;
+    double acc;
+    bool valid;
+  };
+  static const GeoSpoof geo_spoof = []() -> GeoSpoof {
+    const std::string value =
+        base::CommandLine::ForCurrentProcess()->GetSwitchValueASCII(
+            "fingerprint-geolocation");
+    if (value.empty())
+      return {0, 0, 0, false};
     std::vector<std::string> parts = base::SplitString(
-        geo_value, ",", base::TRIM_WHITESPACE, base::SPLIT_WANT_NONEMPTY);
-    if (parts.size() >= 2) {
-      double lat, lon;
-      if (base::StringToDouble(parts[0], &lat) &&
-          base::StringToDouble(parts[1], &lon)) {
-        double acc = 100.0;
-        if (parts.size() >= 3)
-          base::StringToDouble(parts[2], &acc);
-
-        is_running_ = true;
-
-        base::SingleThreadTaskRunner::GetCurrentDefault()->PostTask(
-            FROM_HERE,
-            base::BindOnce(
-                [](LocationProviderManager* self, double lat, double lon,
-                   double acc) {
-                  auto position = mojom::Geoposition::New();
-                  position->latitude = lat;
-                  position->longitude = lon;
-                  position->accuracy = acc;
-                  position->timestamp = base::Time::Now();
-
-                  self->location_update_callback_.Run(
-                      self, mojom::GeopositionResult::NewPosition(
-                                std::move(position)));
-                },
-                base::Unretained(this), lat, lon, acc));
-        return;
-      }
-    }
+        value, ",", base::TRIM_WHITESPACE, base::SPLIT_WANT_NONEMPTY);
+    if (parts.size() < 2)
+      return {0, 0, 0, false};
+    double lat, lon;
+    if (!base::StringToDouble(parts[0], &lat) ||
+        !base::StringToDouble(parts[1], &lon))
+      return {0, 0, 0, false};
+    double acc = 100.0;
+    if (parts.size() >= 3)
+      base::StringToDouble(parts[2], &acc);
+    return {lat, lon, acc, true};
+  }();
+
+  if (geo_spoof.valid) {
+    is_running_ = true;
+    base::SingleThreadTaskRunner::GetCurrentDefault()->PostTask(
+        FROM_HERE,
+        base::BindOnce(
+            [](LocationProviderManager* self, double lat, double lon,
+               double acc) {
+              auto position = mojom::Geoposition::New();
+              position->latitude = lat;
+              position->longitude = lon;
+              position->accuracy = acc;
+              position->timestamp = base::Time::Now();
+              self->location_update_callback_.Run(
+                  self, mojom::GeopositionResult::NewPosition(
+                            std::move(position)));
+            },
+            base::Unretained(this), geo_spoof.lat, geo_spoof.lon,
+            geo_spoof.acc));
+    return;
   }
   // --- END geolocation spoofing ---
   
diff --git a/third_party/blink/renderer/modules/canvas/canvas2d/canvas_2d_recorder_context.cc b/third_party/blink/renderer/modules/canvas/canvas2d/canvas_2d_recorder_context.cc
index fe125c85cb..31df2081c1 100644
--- a/third_party/blink/renderer/modules/canvas/canvas2d/canvas_2d_recorder_context.cc
+++ b/third_party/blink/renderer/modules/canvas/canvas2d/canvas_2d_recorder_context.cc
@@ -153,8 +153,7 @@
 #include "ui/gfx/geometry/skia_conversions.h"
 #include "ui/gfx/geometry/vector2d_f.h"
 #include "v8/include/v8-local-handle.h"
-#include "base/command_line.h"
-#include "components/nodriver4j/nodriver4j_switches.h"
+
 
 // UMA Histogram macros trigger a bug in IWYU.
 // https://github.com/include-what-you-use/include-what-you-use/issues/1546
diff --git a/third_party/blink/renderer/modules/canvas/canvas2d/canvas_path.cc b/third_party/blink/renderer/modules/canvas/canvas2d/canvas_path.cc
index 4521fa2054..6f23ae5cd9 100644
--- a/third_party/blink/renderer/modules/canvas/canvas2d/canvas_path.cc
+++ b/third_party/blink/renderer/modules/canvas/canvas2d/canvas_path.cc
@@ -182,10 +182,12 @@ void CanvasPath::quadraticCurveTo(double double_cpx,
 
   // Sub-pixel curve offset for canvas fingerprint noise
   auto [noise_dx, noise_dy] = GetCanvasNoiseOffset();
-  cpx += noise_dx;
-  cpy += noise_dy;
-  x += noise_dx;
-  y += noise_dy;
+  if (noise_dx != 0.0f) {
+    cpx += noise_dx;
+    cpy += noise_dy;
+    x += noise_dx;
+    y += noise_dy;
+  }
 
   if (!std::isfinite(cpx) || !std::isfinite(cpy) || !std::isfinite(x) ||
       !std::isfinite(y)) [[unlikely]] {
@@ -222,12 +224,14 @@ void CanvasPath::bezierCurveTo(double double_cp1x,
 
   // Sub-pixel curve offset for canvas fingerprint noise
   auto [noise_dx, noise_dy] = GetCanvasNoiseOffset();
-  cp1x += noise_dx;
-  cp1y += noise_dy;
-  cp2x += noise_dx;
-  cp2y += noise_dy;
-  x += noise_dx;
-  y += noise_dy;
+  if (noise_dx != 0.0f) {
+    cp1x += noise_dx;
+    cp1y += noise_dy;
+    cp2x += noise_dx;
+    cp2y += noise_dy;
+    x += noise_dx;
+    y += noise_dy;
+  }
   
   if (!std::isfinite(cp1x) || !std::isfinite(cp1y) || !std::isfinite(cp2x) ||
       !std::isfinite(cp2y) || !std::isfinite(x) || !std::isfinite(y))
@@ -473,8 +477,11 @@ void CanvasPath::arc(double double_x,
   float y = base::saturated_cast<float>(double_y);
   // Sub-pixel arc center offset for canvas fingerprint noise
   auto [noise_dx, noise_dy] = GetCanvasNoiseOffset();
-  x += noise_dx;
-  y += noise_dy;
+  if (noise_dx != 0.0f) {
+    x += noise_dx;
+    y += noise_dy;
+  }
+
 
   float radius = base::saturated_cast<float>(double_radius);
   float start_angle = base::saturated_cast<float>(double_start_angle);
diff --git a/third_party/blink/renderer/modules/mediastream/media_devices.cc b/third_party/blink/renderer/modules/mediastream/media_devices.cc
index 0433492298..169f7e9c8a 100644
--- a/third_party/blink/renderer/modules/mediastream/media_devices.cc
+++ b/third_party/blink/renderer/modules/mediastream/media_devices.cc
@@ -1371,19 +1371,34 @@ void MediaDevices::DevicesEnumerated(
   ReportCompletedEnumerateDevices(result_contains_nonempty_input_device_ids);
 
   // Media device count spoofing via --fingerprint-media=mics,webcams,speakers
-  std::string media_switch =
-      base::CommandLine::ForCurrentProcess()->GetSwitchValueASCII(
-          switches::kFingerprintMedia);
-  if (!media_switch.empty()) {
+    struct MediaSpoof {
+    uint32_t mics;
+    uint32_t webcams;
+    uint32_t speakers;
+    bool active;
+  };
+  static const MediaSpoof media_spoof = []() -> MediaSpoof {
+    std::string value =
+        base::CommandLine::ForCurrentProcess()->GetSwitchValueASCII(
+            switches::kFingerprintMedia);
+    if (value.empty())
+      return {0, 0, 0, false};
     std::vector<std::string> parts = base::SplitString(
-        media_switch, ",", base::TRIM_WHITESPACE, base::SPLIT_WANT_ALL);
-    uint32_t num_mics = 1, num_webcams = 1, num_speakers = 1;
+        value, ",", base::TRIM_WHITESPACE, base::SPLIT_WANT_ALL);
+    uint32_t m = 1, w = 1, s = 1;
     if (parts.size() >= 1)
-      base::StringToUint(parts[0], &num_mics);
+      base::StringToUint(parts[0], &m);
     if (parts.size() >= 2)
-      base::StringToUint(parts[1], &num_webcams);
+      base::StringToUint(parts[1], &w);
     if (parts.size() >= 3)
-      base::StringToUint(parts[2], &num_speakers);
+      base::StringToUint(parts[2], &s);
+    return {m, w, s, true};
+  }();
+
+  if (media_spoof.active) {
+    uint32_t num_mics = media_spoof.mics;
+    uint32_t num_webcams = media_spoof.webcams;
+    uint32_t num_speakers = media_spoof.speakers;
 
     media_devices.clear();
 
diff --git a/third_party/blink/renderer/modules/speech/speech_synthesis.cc b/third_party/blink/renderer/modules/speech/speech_synthesis.cc
index 09d5af33d6..2e49cdbed6 100644
--- a/third_party/blink/renderer/modules/speech/speech_synthesis.cc
+++ b/third_party/blink/renderer/modules/speech/speech_synthesis.cc
@@ -49,6 +49,18 @@
 #include "components/nodriver4j/default_voices.h"
 #include "components/nodriver4j/nodriver4j_switches.h"
 
+namespace {
+base::span<const nodriver4j::DefaultVoiceEntry> GetFakeVoices() {
+  static const auto voices = []() {
+    std::string platform =
+        base::CommandLine::ForCurrentProcess()->GetSwitchValueASCII(
+            switches::kFingerprintPlatform);
+    return nodriver4j::VoicesForPlatform(platform);
+  }();
+  return voices;
+}
+}  // namespace
+
 namespace blink {
 
 const char SpeechSynthesis::kSupplementName[] = "SpeechSynthesis";
@@ -95,11 +107,7 @@ void SpeechSynthesis::OnSetVoiceList(
 
   // When --fingerprint-platform is set, replace the real OS voice list
   // with a curated platform-appropriate set of fake voices.
-  std::string platform =
-      base::CommandLine::ForCurrentProcess()->GetSwitchValueASCII(
-          switches::kFingerprintPlatform);
-  auto fake_voices = nodriver4j::VoicesForPlatform(platform);
-
+  auto fake_voices = GetFakeVoices();
   if (!fake_voices.empty()) {
     for (const auto& entry : fake_voices) {
       voice_list_.push_back(MakeGarbageCollected<SpeechSynthesisVoice>(
@@ -271,11 +279,7 @@ void SpeechSynthesis::StartSpeakingImmediately() {
   // When voice spoofing is active, all exposed voices are fake.
   // We cannot forward to the real TTS engine (it doesn't know these voices),
   // so we dispatch synthetic start/end events with realistic timing.
-  std::string platform =
-      base::CommandLine::ForCurrentProcess()->GetSwitchValueASCII(
-          switches::kFingerprintPlatform);
-  auto fake_voices = nodriver4j::VoicesForPlatform(platform);
-
+  auto fake_voices = GetFakeVoices();
   if (!fake_voices.empty()) {
     // Fire the "start" event synchronously (matches real TTS behavior —
     // the start event fires almost immediately after speak() is called).
@@ -287,7 +291,7 @@ void SpeechSynthesis::StartSpeakingImmediately() {
     if (rate <= 0.0f)
       rate = 1.0f;
     float duration_seconds = static_cast<float>(text_length) /
-                             (nodriver4j::FakeSpeechCharsPerSecond() * rate);
+                             (nodriver4j::kFakeSpeechCharsPerSecond * rate);
 
     // Schedule the "end" event after the calculated duration.
     // WrapWeakPersistent guards against GC of |this| (e.g. if the page
diff --git a/third_party/blink/renderer/modules/webgl/webgl2_rendering_context_base.cc b/third_party/blink/renderer/modules/webgl/webgl2_rendering_context_base.cc
index 82929d4362..7e7dd28983 100644
--- a/third_party/blink/renderer/modules/webgl/webgl2_rendering_context_base.cc
+++ b/third_party/blink/renderer/modules/webgl/webgl2_rendering_context_base.cc
@@ -4870,10 +4870,11 @@ ScriptValue WebGL2RenderingContextBase::getParameter(ScriptState* script_state,
     case GL_MAX_VERTEX_UNIFORM_COMPONENTS: {
       const base::CommandLine* cl = base::CommandLine::ForCurrentProcess();
       if (cl->HasSwitch(switches::kFingerprintGpuVendor)) {
-        std::string vendor =
-        cl->GetSwitchValueASCII(switches::kFingerprintGpuVendor);
         GLint real_value = 0;
         ContextGL()->GetIntegerv(GL_MAX_VERTEX_UNIFORM_COMPONENTS, &real_value);
+        (void)real_value;
+        std::string vendor =
+        cl->GetSwitchValueASCII(switches::kFingerprintGpuVendor);
         if (vendor.find("NVIDIA") != std::string::npos) {
           return WebGLAny(script_state, 16380);
         }
diff --git a/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc b/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
index 3bc5dd95c5..fbab1b377c 100644
--- a/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
+++ b/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
@@ -3867,10 +3867,11 @@ ScriptValue WebGLRenderingContextBase::getParameter(ScriptState* script_state,
     case GL_MAX_VERTEX_UNIFORM_VECTORS: {
       const base::CommandLine* cl = base::CommandLine::ForCurrentProcess();
       if (cl->HasSwitch(switches::kFingerprintGpuVendor)) {
-        std::string vendor =
-          cl->GetSwitchValueASCII(switches::kFingerprintGpuVendor);
         GLint real_value = 0;
         ContextGL()->GetIntegerv(GL_MAX_VERTEX_UNIFORM_VECTORS, &real_value);
+        (void)real_value;
+        std::string vendor =
+          cl->GetSwitchValueASCII(switches::kFingerprintGpuVendor);
         if (vendor.find("NVIDIA") != std::string::npos) {
           return WebGLAny(script_state, 4095);
         }
diff --git a/third_party/blink/renderer/platform/fonts/shaping/harfbuzz_shaper.cc b/third_party/blink/renderer/platform/fonts/shaping/harfbuzz_shaper.cc
index abdb3c03df..fe529ac453 100644
--- a/third_party/blink/renderer/platform/fonts/shaping/harfbuzz_shaper.cc
+++ b/third_party/blink/renderer/platform/fonts/shaping/harfbuzz_shaper.cc
@@ -1242,7 +1242,6 @@ void HarfBuzzShaper::GetGlyphData(const SimpleFontData& font_data,
       platform_data.size());
   DCHECK(hb_font);
   hb_shape(hb_font, hb_buffer, nullptr, 0);
-  ApplyFontSpacingNoise(hb_buffer);
 
   // Create `GlyphDataList` from `hb_buffer`.
   unsigned num_glyphs;
-- 
2.45.1.windows.1

